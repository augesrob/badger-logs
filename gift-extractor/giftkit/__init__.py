"""giftkit - pull TikTok LIVE gift animations and convert them to alpha video.

Public entry points live in :mod:`giftkit.cli`; the stages are usable as a
library too (``sources`` -> ``manifest`` -> ``convert`` -> ``library``).
"""

__version__ = "0.1.0"
